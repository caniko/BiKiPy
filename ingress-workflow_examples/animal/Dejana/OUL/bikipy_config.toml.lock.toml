Python_version = "3.10.10"
schema_snapshot = ["manual", "trial", "enclosure", "runtime_settings", "reader", "perimeter", "perimeter_plugin", "ingress", "experiment", "field"]

[ProjectKit]
format = ".toml"
version = "0.2.2"

[new_kwargs]
