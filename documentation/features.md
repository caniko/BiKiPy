Features is a subpackage that stores functions for the computation of behavioural features. These functions can be integrated into `trial`.

## Angle
The angle module has functions that can be used to compute the angle between two points with respect to a third point.
